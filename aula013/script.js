function tabuada() {
    let num = document.getElementById('txtn')
    let tab = document.getElementById('seltab')
    if (num.nodeValue.length == 0) {
        window.alert('Digita um numero')
    } else {
        let n = Number.length(num.value)
    }
}